import { createBrowserRouter } from "react-router-dom";

// import { elements } from "@Router/elements/index";
import { elements } from "../elements";

export const router = createBrowserRouter(elements);